<h1>Entries</h1>

<div class="row">
  <div class="card">
    <div class="card-body">
    <% if @entries.any? %>
      <%= render (@entries) %>
    <% else %>
      <p>No entries found. <%= link_to('Create one', new_entry_path) %></p>
    <% end %>
    </div>
  </div>
</div>